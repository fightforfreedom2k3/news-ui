import { Stack } from '@mui/material';

export default function Footer() {
    return (
        <Stack sx={{ backgroundColor: 'black' }}>
            <Stack width={1300} pt={2}></Stack>
        </Stack>
    );
}
